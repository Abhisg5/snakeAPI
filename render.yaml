services:
  - type: web
    name: snake-game-api
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd src/core && make
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: PORT
        value: 4000 